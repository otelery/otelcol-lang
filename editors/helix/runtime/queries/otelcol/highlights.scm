; otelcol highlights — inherit from the bundled YAML query set.
; The LSP server provides semantic tokens for component-type vs id
; classification; the tree-sitter layer just needs vanilla YAML
; highlighting underneath.

; inherits: yaml
