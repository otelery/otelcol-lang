# Resolve platform-specific optionalDependencies (esbuild, rollup, …) for
# every target on every `npm install`, not just whichever host happens to run
# it. Without this, package-lock.json loses non-host variants and CI breaks on
# other platforms.
os=linux,darwin,win32
cpu=x64,arm64
